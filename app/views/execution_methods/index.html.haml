.p.mb-2
  = link_to "New", [:new, :execution_method], class: 'btn btn-outline-primary'

= bootstrap_will_paginate @items

%table.table.table-bordered.table-sm
  = render "list_header"
  = render partial: "item", collection: @items

= bootstrap_will_paginate @items
